A `build collection` is a collection of project settings that determines how your IDE will build the project. Ex. Name of project, what directories, how much to optimize.

Typically ide sets up a release and a debug config.

Debug: use when writing programs. turns off optimizations and includes debugging information
Release: release to public. optimized for size and performance. Doesnt contain any debugging information.